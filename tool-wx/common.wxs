var tool = {
   computedDateState: function(arr,target){
     console.log("回去",arr);
     console.log("目标时间",target)
      for(var i = 0; i < arr.length;i++){
          if(arr[i].idFlag === target){
            return arr[i].isDo
          }
      }
  }
}
module.exports = {
  computedDateState: tool.computedDateState
}